apiVersion: v1
kind: Service
metadata:
  name: {{ include "mywebserverstarter.fullname" . }}
  labels:
    {{- include "mywebserverstarter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mywebserverstarter.selectorLabels" . | nindent 4 }}
