apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mywebserverstarter.fullname" . }}-test-connection"
  labels:
    {{- include "mywebserverstarter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mywebserverstarter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
